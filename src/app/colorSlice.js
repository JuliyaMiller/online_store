import { createSlice } from "@reduxjs/toolkit";

const WhiteSchema = {
    textColor: '#fff',
    backgraounColor: '#ccc',
    optinalColor: '#ff0'
}

const initialState = {
    schema: null,
    textColor: null,
    backgraounColor: null,
    optinalColor: null
}